diff --git a/libobs-metal/MTLOrigin+Extensions.swift b/libobs-metal/MTLOrigin+Extensions.swift
index 2d160ef..c782fa9 100644
--- a/libobs-metal/MTLOrigin+Extensions.swift
+++ b/libobs-metal/MTLOrigin+Extensions.swift
@@ -18,7 +18,7 @@
 import Foundation
 import Metal
 
-extension MTLOrigin: @retroactive Equatable {
+extension MTLOrigin: Equatable {
     public static func == (lhs: MTLOrigin, rhs: MTLOrigin) -> Bool {
         lhs.x == rhs.x && lhs.y == rhs.y && lhs.z == rhs.z
     }
diff --git a/libobs-metal/MTLRegion+Extensions.swift b/libobs-metal/MTLRegion+Extensions.swift
index afa3632..3cc763b 100644
--- a/libobs-metal/MTLRegion+Extensions.swift
+++ b/libobs-metal/MTLRegion+Extensions.swift
@@ -18,7 +18,7 @@
 import Foundation
 import Metal
 
-extension MTLRegion: @retroactive Equatable {
+extension MTLRegion: Equatable {
     public static func == (lhs: MTLRegion, rhs: MTLRegion) -> Bool {
         lhs.origin == rhs.origin && lhs.size == rhs.size
     }
diff --git a/libobs-metal/MTLSize+Extensions.swift b/libobs-metal/MTLSize+Extensions.swift
index 0010979..1a2fa48 100644
--- a/libobs-metal/MTLSize+Extensions.swift
+++ b/libobs-metal/MTLSize+Extensions.swift
@@ -18,7 +18,7 @@
 import Foundation
 import Metal
 
-extension MTLSize: @retroactive Equatable {
+extension MTLSize: Equatable {
     public static func == (lhs: MTLSize, rhs: MTLSize) -> Bool {
         lhs.width == rhs.width && lhs.height == rhs.height && lhs.depth == rhs.depth
     }
diff --git a/libobs-metal/MTLViewport+Extensions.swift b/libobs-metal/MTLViewport+Extensions.swift
index 97c9a9c..b6451d7 100644
--- a/libobs-metal/MTLViewport+Extensions.swift
+++ b/libobs-metal/MTLViewport+Extensions.swift
@@ -18,7 +18,7 @@
 import Foundation
 import Metal
 
-extension MTLViewport: @retroactive Equatable {
+extension MTLViewport: Equatable {
     /// Checks two ``MTLViewPort`` objects for equality
     /// - Parameters:
     ///   - lhs: First ``MTLViewPort``object
diff --git a/libobs-metal/MetalDevice.swift b/libobs-metal/MetalDevice.swift
index d3fa017..b61df38 100644
--- a/libobs-metal/MetalDevice.swift
+++ b/libobs-metal/MetalDevice.swift
@@ -61,11 +61,7 @@ class MetalDevice {
         let nopVertexSource = "[[vertex]] float4 vsNop() { return (float4)0; }"
 
         let compileOptions = MTLCompileOptions()
-        if #available(macOS 15, *) {
-            compileOptions.mathMode = .fast
-        } else {
-            compileOptions.fastMathEnabled = true
-        }
+        compileOptions.fastMathEnabled = true
 
         guard let library = try? device.makeLibrary(source: nopVertexSource, options: compileOptions),
             let function = library.makeFunction(name: "vsNop")
diff --git a/libobs-metal/OBSShader.swift b/libobs-metal/OBSShader.swift
index a53bb0c..0debf8b 100644
--- a/libobs-metal/OBSShader.swift
+++ b/libobs-metal/OBSShader.swift
@@ -542,7 +542,7 @@ class OBSShader {
                 textures: [],
                 samplers: [],
                 arguments: [],
-                gsFunction: function,
+                gsFunction: function
             )
 
             for j in 0..<function.pointee.params.num {
diff --git a/plugins/mac-avcapture/OBSAVCapture.m b/plugins/mac-avcapture/OBSAVCapture.m
index 29ce8e9..024c139 100644
--- a/plugins/mac-avcapture/OBSAVCapture.m
+++ b/plugins/mac-avcapture/OBSAVCapture.m
@@ -775,7 +775,10 @@ static const UInt32 kMaxFrameRateRangesInDescription = 10;
         }
     }
     if (@available(macOS 15.0, *)) {
-        if (device.backgroundReplacementActive) {
+        SEL backgroundReplacementActiveSelector = @selector(backgroundReplacementActive);
+        BOOL (*backgroundReplacementActiveFn)(id, SEL) = (BOOL (*)(id, SEL))[device methodForSelector:backgroundReplacementActiveSelector];
+        if ([device respondsToSelector:backgroundReplacementActiveSelector] && backgroundReplacementActiveFn &&
+            backgroundReplacementActiveFn(device, backgroundReplacementActiveSelector)) {
             effectWarning = @"Warning.Effect.BackgroundReplacement";
             effectsCount++;
         }
diff --git a/plugins/mac-videotoolbox/encoder.c b/plugins/mac-videotoolbox/encoder.c
index acd94c6..e6e12d5 100644
--- a/plugins/mac-videotoolbox/encoder.c
+++ b/plugins/mac-videotoolbox/encoder.c
@@ -576,6 +576,9 @@ static OSStatus create_encoder(struct vt_encoder *enc)
 			return code;
 		}
 
+#if defined(kVTCompressionPropertyKey_SpatialAdaptiveQPLevel) \
+    && defined(kVTQPModulationLevel_Default) \
+    && defined(kVTQPModulationLevel_Disable)
 		if (__builtin_available(macOS 15.0, *)) {
 			int spatial_aq = enc->spatial_aq ? kVTQPModulationLevel_Default : kVTQPModulationLevel_Disable;
 			CFNumberRef spatialAQ = CFNumberCreate(kCFAllocatorDefault, kCFNumberIntType, &spatial_aq);
@@ -589,6 +592,7 @@ static OSStatus create_encoder(struct vt_encoder *enc)
 
 			CFRelease(spatialAQ);
 		}
+#endif
 	}
 
 	// This can fail depending on hardware configuration
